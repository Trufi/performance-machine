import * as React from 'react';
import * as ReactDOM from 'react-dom';

ReactDOM.render(
    <h1>Hello!!!</h1>,
    document.getElementById('root'),
);

export function renderAggregatorData(data: any) {
    console.log(data);
}
